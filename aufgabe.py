# Aufgabe: Erstelle einen Observer mit dem Namen "RAMObserver" zur Überwachung des RAMs auf dem PC.
# Der Observer soll in der Datei logic.py implementiert werden und in der Datei aufgabe.py aufgerufen werden.
# Anschließend soll in der Datei aufgabe.py ein Subject mit dem Namen "RAMSubject" erstellt werden, um den RAM zu überwachen.
# Achte darauf, so wenig wie möglich die main.py-Datei zur Hilfe zu nehmen; konzentriere dich hauptsächlich auf logic.py und interfaces.py.
